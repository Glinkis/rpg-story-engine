import { Roll } from "./types"

export const defaultExpertiseRolls: Roll[] = [
  [80, `masterful`],
  [70, `exceptional`],
  [60, `superior quality`],
  [50, `finely crafted`],
  [40, `well crafted`],
  [30, `sloppily made`],
  [20, `somewhat amateur`],
  [10, `amateur`],
  [0, `blatantly amateur`],
]
