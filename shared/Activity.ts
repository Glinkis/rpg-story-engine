/**
 * TODO: Add description.
 */
export enum Activity {
  ExtremelyBusy = `extremely busy`,
  VeryBusy = `very busy`,
  RatherBusy = `rather busy`,
  ReasonablyBusy = `reasonably busy`,
  NotTerriblyBusy = `not terribly busy`,
  NotBusy = `not busy`,
  RatherQuiet = `rather quiet`,
  VeryQuiet = `very quiet`,
}
